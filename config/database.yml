development:
  adapter: postgresql
  encoding: unicode
  database: pressevent_development
  pool: 5
  host: localhost
  username: postgres
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: unicode
  database: pressevent_test
  host: localhost
  pool: 5
  username: postgres
  password:

# The production database is setup by heroku hooks
